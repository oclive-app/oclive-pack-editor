{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pack-editor-main",
  "description": "Narrow permissions for the role-pack editor main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
